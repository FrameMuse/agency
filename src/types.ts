export interface Project {
  name: string
  description: string
  status: string
  tags: string
  price: string
  hours: string
  links: string[]
  series?: string[]
  highlights: string[]
  images: string[]
}
